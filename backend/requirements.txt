fastapi==0.115.0
uvicorn[standard]==0.30.6
psycopg2-binary==2.9.9
python-dotenv==1.0.1
httpx==0.27.0
sse-starlette==2.1.3
openpyxl==3.1.5
# Playwright drives Chromium for the Snapshots panel only (snapshot_scraper.py).
# Structured product/seller data still flows through BrightData (pw_scraper.py).
playwright==1.47.0
